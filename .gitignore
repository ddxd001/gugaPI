# CCS Theia 构建输出目录（SysConfig + gmake 生成，全部可重新生成）
Debug/
Release/

# 目标文件与依赖文件
*.o
*.d
*.obj

# 链接 / 映射 / 可烧录输出
*.map
*.out
*.genlibs
*.hex
*.bin

# SysConfig 生成的编译数据库
compile_commands.json

# 编辑器与系统临时文件
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# 本地参考资料与临时文件（数据手册截图等，不纳入版本库）
FOC/tmp/

# 机器特定的本地配置（含绝对路径和本地权限设置）
.claude/settings.local.json
.theia/launch.json
